{
    "configurations": [
        {
            "name": "CMake",
            "compileCommands": "${workspaceFolder}/build-release/compile_commands.json",
            "includePath": [
                "${default}",
                "${workspaceFolder}/build-release/proto",
                "/usr/include/userver"
            ]
        }
    ],
    "version": 4
}